{
  "name": "tales-from-the-qa-crypt",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-scripts": "1.1.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "mockApi": "json-server -c db/json-server.json db/db.json"
  },
  "devDependencies": {
    "cypress": "^1.4.1",
    "json-server": "^0.12.1"
  }
}
